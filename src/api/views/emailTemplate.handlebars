<!DOCTYPE html>
<html>
  <head>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            line-height: 1.6;
            color: #333;
        }
        .header {
            background-color: #28a745;
            padding: 15px;
            border-radius: 8px 8px 0 0;
            text-align: center;
            color: white;
            font-size: 24px;
        }
        .content {
            padding: 20px;
        }
        .code {
            display: inline-block;
            padding: 10px 20px;
            font-size: 24px;
            color: white;
            background-color: #28a745;
            border-radius: 5px;
            margin: 20px 0;
            text-align: center;
        }
        p {
            margin: 10px 0;
        }

        .btn {
          display: inline-block;
          background-color: #007bff;
          color: white;
          padding: 10px 20px;
          text-align: center;
          text-decoration: none;
          font-weight: bold;
          border-radius: 5px;
          transition: background-color 0.3s ease;
        }
        
        .btn:hover {
          background-color: #0056b3;
        }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        {{#if isVerification}}Email Verification{{else}}Password Reset{{/if}}
      </div>
      <div class="content">
        <p>Dear Customer,</p>

        {{#if isVerification}}
        <p>Thank you for registering with PRO2052 Restaurant. Please use the
          following verification code to complete your registration:</p>
        {{else}}
        <p>You have requested to reset your password. This request will expire
          in 1 minute. Please use the following code to reset your password:</p>
        {{/if}}

        <div style="text-align: center;">
          {{#if isVerification}}
          <div class="code">{{verificationCode}}</div>
          {{else}}
          <a href="{{resetURL}}" class="btn">Reset Password</a>
          {{/if}}
        </div>

        <p>If you did not request this, please ignore this email.</p>
        <p>Best regards,<br />PRO2052 Restaurant Team</p>
      </div>
    </div>
  </body>
</html>
